.data
.text
.globl main
main:
    # Start Test
    xor $1, $1, $1         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $2, $2, $2         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $3, $3, $3         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $4, $4, $4         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $5, $5, $5         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $6, $6, $6         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $7, $7, $7         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $8, $8, $8         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $9, $9, $9         # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $10, $10, $10     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $11, $11, $11     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $12, $12, $12     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $13, $13, $13     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $14, $14, $14     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $15, $15, $15     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $16, $16, $16     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $17, $17, $17     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $18, $18, $18     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $19, $19, $19     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $20, $20, $20     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $21, $21, $21     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $22, $22, $22     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $23, $23, $23     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $24, $24, $24     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $25, $25, $25     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $26, $26, $26     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $27, $27, $27     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $28, $28, $28     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $29, $29, $29     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $30, $30, $30     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    xor $31, $31, $31     # verify that xor can clear registers since a register xor-ed with itself result in 0   
    # End Test
    # Exit program
    halt
